/**
 * POS "Renew license" deep link targets (marketing / support funnel).
 */

/** Public site opened in-system browser via expo-web-browser. */
export const LICENSE_RENEWAL_URL = 'https://www.regkasse.at';

/** Shown when the browser session cannot start (fallback Alert). */
export const LICENSE_SUPPORT_EMAIL = 'support@regkasse.at';
