# Risk → Control → Evidence → Remaining gap

This table collects the controls and evidence from the technical review in one place. Columns: **Risk**, **Existing control / new control**, **Code file**, **Test / SQL / document evidence**, **Evidence level**, **Remaining open risk**.

---

| Risk | Existing control / new control | Code file | Test / SQL / document evidence | Evidence level | Remaining open risk |
|------|--------------------------------|-----------|--------------------------------|----------------|---------------------|
| **Concurrent replay correctness** | Unique index `(CashRegisterId, payload_hash)` + advisory lock so the same payload yields one payment / one offline row. **New:** Validation script checks that this index exists; test-class summary references the evidence. | `OfflineTransactionService.cs`, `AppDbContext.cs` (unique index definition) | `PostgreSqlOfflineReplayConcurrencyTests.cs`: `ConcurrentReplay_SameRegisterSameOfflineId_ProducesSinglePayment`, `ConcurrentReplay_DifferentOfflineIdsSamePayload_OneCanonicalPayment`. `scripts/sql/fiscal_go_live_validation.sql`: `idx_offline_transactions_cash_register_payload_hash_unique`. | High | Concurrent replays to the same cash register on a real multi-node production deploy are proven only on a single test DB. Network partition / split-brain is out of test scope. |
| **Advisory lock effectiveness** | Before replay, `OfflineReplayRegisterLock.AcquireAsync` takes a register-scoped `pg_advisory_lock`. A second acquire on the same cash register blocks until the first is released. | `OfflineReplayRegisterLock.cs`, `OfflineTransactionService.cs` | `PostgreSqlOfflineReplayConcurrencyTests.cs`: `AdvisoryLock_SecondAcquireWaitsUntilFirstScopeDisposed` (second acquire waits ~350ms). | High | No lock timeout. A long-running replay can make another instance wait indefinitely. |
| **Idempotency uniqueness** | Unique partial index on payment `idempotency_key` (IS NOT NULL); cancel/refund uses a separate key. On a race, a duplicate insert is caught as 23505 and the existing payment is returned. **New:** Validation script checks the idempotency index and duplicate-key data. | `PaymentService.cs`, `AppDbContext.cs` | `PostgreSqlOfflineReplayConcurrencyTests.cs`: `ConcurrentCreatePayment_SameIdempotencyKey_SingleRow_PostgresUniqueIndex` (20 parallel, one row). `fiscal_go_live_validation.sql`: `idx_payment_idempotency_partial_unique`, `dup_payment_idempotency_key`. | High | Cancel/refund idempotency keys use a separate index. The same concurrency test does not cover cancel/refund. |
| **Signature chain continuity** | TSE signature chain is serialized per cash register with FOR UPDATE; chain fields are stored on the receipt. Export checks the chain inside the export window (best-effort). | `TseService.cs`, `ReceiptService.cs`, `FiscalExportService.cs` | `PostgreSqlOfflineReplayConcurrencyTests.cs`: `ConcurrentPayments_SameRegister_RealTse_UniqueBelegNrAndMonotonicChain`. `FiscalExportIntegrityMetricsTests.cs`: signature chain valid/invalid scenarios. `FISCAL_EXPORT_DIAGNOSTICS.md`: the chain field is diagnostic. | Medium | Export chain check is “observed-within-scope” only. Full cash-register history or multiple registers in one export is not proven. Real TSE hardware is out of test scope. |
| **Receipt sequencing** | `receipt_sequences` table unique on (cash_register_id, sequence_date). Daily Beleg number is taken from this table via UPSERT. | `ReceiptSequenceService.cs`, `AppDbContext.cs` | `PostgreSqlOfflineReplayConcurrencyTests.cs`: `ConcurrentPayments_SameRegister_RealTse_UniqueBelegNrAndMonotonicChain` (n payments, n distinct ReceiptNumber values). `fiscal_go_live_validation.sql`: `idx_receipt_sequences_unique`, `dup_receipt_sequences_per_register_day`, `dup_payment_receipt_number`, `dup_receipts_receipt_number`. | High | Sequence gap/skip (for example after an error + rollback) is not a separate test. Validation prevents duplicates, not gaps. |
| **Migration drift / orphan risk** | Pre-go-live read-only validation script: column drift (legacy column absent, required column present), unique indexes, duplicate data, orphans (receipt→payment, payment→cash_register, invoice→payment, and so on), offline payload_hash coverage. **New:** Offline unique-index check added. | — | `scripts/sql/fiscal_go_live_validation.sql` (all check_id values). Run: `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/sql/fiscal_go_live_validation.sql`. | Medium | Script must be run manually or in CI after migrations. There is no automatic rollback. New migrations may not add matching rules to the script. |
| **Export integrity semantics** | All integrity booleans on the export are defined as “diagnostic, best-effort, observed-within-scope.” They are not a guarantee or legal proof. | `FiscalExportService.cs`, `FiscalExportController.cs`, DTOs | `docs/release/FISCAL_EXPORT_DIAGNOSTICS.md`, `FISCAL_EXPORT_TERMINOLOGY_GUIDE.md`. In-service comment: "All integrity booleans in this block are diagnostic (best-effort, observed-within-scope only); they do not constitute a legal or global guarantee." | Medium | The UI may not make this distinction clear. Copy may not be fully translated to German. |
| **Legacy payload-hash mismatch** | Runtime canonical hash (sorted keys) ≠ older backfill digest(text). Measure: analyze endpoint. Repair: bulk repair API + lazy align during replay. **New:** Measure/repair collected in one document. Validation script already checked offline index + null/coverage/duplicate. | `OfflinePayloadHashMaintenanceService.cs`, `OfflineTransactionService.cs` (TryAlignStoredPayloadHashToRuntimeCanonicalAsync), `OfflinePayloadHashMaintenanceController.cs` | `docs/release/LEGACY_PAYLOAD_HASH_MISMATCH.md`. API: `POST /api/admin/offline-payload-hash/analyze`, `.../repair` (DryRun first). `scripts/sql/offline_payload_hash_legacy.sql` (informational). `fiscal_go_live_validation.sql`: offline payload_hash null/coverage/dup. | Medium | On conflict (canonical hash already on another row for the same cash register), repair is skipped. Those rows can stay on the legacy hash. |
| **Old mobile device/sequence coverage** | A sample of replayed intents is stored in `offline_intent_coverage_samples` and watched via an admin endpoint for DeviceId/ClientSequence presence. | `OfflineTransactionService.cs`, `OfflineIntentCoverageController.cs`, `OfflineIntentCoverageSample` | `GET /api/admin/offline-intent-coverage`. `docs/release/DEVICE_SEQUENCE_COVERAGE.md`. | Low | Metric is observational. There is no per-device breakdown of old-build share and no automatic action. |
| **Post-commit external submit divergence** | FinanzOnline submit status and error live on PaymentDetails. Retry and the reconciliation endpoint can detect divergence. | `PaymentService.cs` (RetryFinanzOnlineSubmitAsync), `FinanzOnlineService.cs`, `FinanzOnlineReconciliationController.cs`, `PaymentDetails` (FO columns) | `FinanzOnlineReconciliationTests.cs`. `docs/release/FINANZONLINE_RECONCILIATION.md`. Reconciliation API: submitted/failed/retry counts and sample rows. | Medium | No automatic alerting (for example failed > threshold) or periodic job in this evidence row; ops depends on a manual/extra dashboard. |

---

## Abbreviations / notes

- **Evidence level:** High = verified with real PostgreSQL/Testcontainers or production-like SQL. Medium = a test or script exists but scope/environment is limited. Low = observational metric or document.
- **New** = control/document added or clarified in this session (backlog, validation SQL offline index, legacy payload-hash doc, test summary).
- All code paths are under `backend/`. Documents are under `docs/release/`.
