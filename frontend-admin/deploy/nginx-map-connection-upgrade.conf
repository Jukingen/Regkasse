# Map snippet required by nginx.conf WebSocket proxying.
# Include from the main http {} block, e.g.:
#   include /etc/nginx/conf.d/regkasse-admin-map.conf;

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
