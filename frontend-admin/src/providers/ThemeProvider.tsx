'use client';

/**
 * Theme + density shell: localStorage, API sync, Ant Design ConfigProvider, system preference listener.
 * @see acceptance: theme persistence, OS system theme, compact/comfortable density
 */
export { ThemeProvider } from '@/lib/personalization/ThemeProvider';
