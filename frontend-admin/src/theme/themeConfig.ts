import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
    token: {
        fontSize: 14,
        colorPrimary: '#1677ff',
        borderRadius: 6,
    },
    components: {
        Layout: {
            bodyBg: '#f5f5f5',
            headerBg: '#ffffff',
        },
    },
};

export default theme;
