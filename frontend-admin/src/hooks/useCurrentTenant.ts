/**
 * Re-export for `@/hooks/useCurrentTenant` imports.
 * Implementation: `@/features/tenancy/hooks/useCurrentTenant`.
 */
export { useCurrentTenant } from '@/features/tenancy/hooks/useCurrentTenant';
