'use client';

/**
 * Phase 1 plan path alias — canonical widget lives under `widgets/BackupStatusWidget`.
 */
export { BackupStatusWidget as BackupWidget, BackupStatusWidget } from '@/features/dashboard/widgets/BackupStatusWidget';
