'use client';

/**
 * Phase 1 plan path alias — canonical widget lives under `widgets/BackupStatusWidget`.
 */
export {
  BackupStatusWidget,
  BackupStatusWidget as BackupWidget,
} from '@/features/dashboard/widgets/BackupStatusWidget';
