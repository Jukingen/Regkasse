import { test } from '@playwright/test';

/**
 * Placeholder for backup trigger / history E2E.
 * Enable when a stable staging stack (or dedicated API mocks) is available.
 */
test.describe.skip('Backup trigger and view', () => {
  test('triggers a backup and shows the run in the list', async () => {
    // Intentionally skipped — implement with E2E_LIVE or extended apiMocks.
  });
});
